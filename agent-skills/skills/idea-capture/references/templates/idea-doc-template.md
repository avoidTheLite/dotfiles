# Idea: <Working Title>

_Produced from an ideation session on <date>. This document maps the explored space — it is a snapshot of options and open questions, not a commitment to build any one of them._

## Core throughline

What stays true across every path or option below — the one or two sentences that would still describe this idea even if every variable below resolved differently.

## Top-level paths

_Only include this section if the session surfaced genuinely different directions the whole project could take. If there was only one coherent path, skip straight to Feature/component-level options._

### Path A: <name>
- What it emphasizes:
- What it trades off / gives up relative to the other paths:
- Where it came from in the conversation (brief):

### Path B: <name>
(same structure)

### Path C: <name>
(same structure, if applicable)

## Feature / component-level options

Local choice points that don't change which top-level path you're on. For each: the decision, the options on the table, and which path(s) it applies to if not all.

| Variable | Options considered | Applies to |
|---|---|---|
| | | |

## Assumptions surfaced

Premises the user was building on that were named as assumptions during the session — not yet validated or rejected, just made visible.

-

## Tensions / incompatibilities identified

Pairs or sets of stated elements that can't coexist in a single build, and how they were resolved (kept as a fork above, or one side dropped).

-

## Open questions

Things raised in the session that don't yet have an answer.

-

## Possible next documents

For each path or major option above, what downstream document (if any) it would feed into once/if it's chosen — PRD, Defect, Behavioral/BDD story, Technical/task story, Agent handoff. This is a pointer for future work, not drafted content.

-
