{
  "description": "Branch naming for this repository. Other repos should copy this file to their own `config/branch-standards.json` (or similar) and set `headBranchPattern` to match that team's rules. The GitHub workflow reads this file so the regex lives in one place per repo.",
  "defaultBranch": "main",
  "headBranchPattern": "^((feature|fix|chore|docs|cursor)/[a-z0-9][a-z0-9-]*|dependabot/.+)$",
  "explanation": "Topic branches: one of the prefixes, a slash, then a kebab-style slug (lowercase letters, digits, hyphens; must not start with a hyphen). The dependabot/ prefix is allowed so automated updates can merge. The cursor/ prefix is allowed for Cloud Agent branches. No underscores in the slug.",
  "examples": [
    "feature/add-feature-name",
    "fix/bug-fix-issue-name",
    "chore/update-dependencies",
    "docs/update-readme",
    "cursor/turbo-plop-monorepo-generator-c41c",
    "dependabot/npm_and_yarn/package-1.0.0"
  ]
}
