import { render, screen } from '@testing-library/react';
import { expect, it } from 'vitest';
import { App } from './App.tsx';

it('renders the generated project heading', () => {
  render(<App />);
  expect(screen.getByRole('heading', { name: /react app is ready/i })).toBeTruthy();
});
