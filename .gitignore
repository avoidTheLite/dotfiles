# Optional local notes that must never be public (see guides/validation/PRIVACY.md)
private/

# Agent prompt replay and inspect artifacts (see identity/generation/DEV_LOOPS.md)
.dotfiles-cache/

# dotenv overrides of config/ports.json
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
*~
*.swp
